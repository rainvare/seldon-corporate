[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "seldon-corporate"
version = "3.0.0"
description = "Organizational resilience framework — dynamic model with Early Warning Signals"
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.10"
dependencies = ["numpy>=1.24.0", "pandas>=2.0.0"]

[project.urls]
Repository = "https://github.com/rainvare/seldon-corporate"
